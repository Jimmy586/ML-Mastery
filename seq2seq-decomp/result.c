#include <stdio.h>
#include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if result \t m;
 min_index;
 #include n2; if